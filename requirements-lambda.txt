# Core dependencies
fastapi==0.109.0
mangum==0.17.0
python-multipart==0.0.6
pydantic>=2.0.0

# Image processing (numpy/scipy installed separately in Dockerfile)
opencv-python-headless==4.9.0.80
Pillow==10.2.0

# PyTorch CPU-only for smaller image size
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu

# Cellpose
cellpose==3.0.8
